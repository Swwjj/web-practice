<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.webproj.mappers.ProductTypeMapper">

    <select id="countChildren" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM product_type WHERE parent_id = #{id}
    </select>

    <select id="countProductsByProductIdOrPartsId" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM product WHERE product_id = #{id} OR parts_id = #{id}
    </select>

    <delete id="deleteById" parameterType="int">
        DELETE FROM product_type WHERE id = #{id}
    </delete>

    <select id="findPathParam" resultType="com.example.webproj.pojo.ProductType">
        SELECT * FROM product_type WHERE level > 0 AND status = 1
    </select>

    <select id="findPartsType" resultType="com.example.webproj.pojo.ProductType" parameterType="int">
        SELECT * FROM product_type WHERE parent_id = #{productTypeId} AND status = 1
    </select>

    <select id="findPType" resultType="com.example.webproj.pojo.ProductType">
        SELECT * FROM product_type WHERE parent_id = 0 AND status = 1
    </select>

    <select id="findChildren" resultType="com.example.webproj.pojo.ProductType" parameterType="int">
        SELECT * FROM product_type WHERE parent_id = #{id} AND status = 1
    </select>

    <update id="update" parameterType="com.example.webproj.pojo.ProductType">
        UPDATE product_type
        SET name = #{name}, updated = #{updated}
        WHERE id = #{id}
    </update>

    <insert id="insert" parameterType="com.example.webproj.pojo.ProductType">
        INSERT INTO product_type (parent_id, name, sort_order, status, level, created, updated)
        VALUES (#{parentId}, #{name}, #{sortOrder}, #{status}, #{level}, #{created}, #{updated})
    </insert>
</mapper>
